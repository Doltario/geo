const state = {
  geo: {},
};

const getters = {
  geo(state) {
    return state.geo;
  },
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
